This folder is the physical project library for RECALL.

If you open the app and click "Connect libarray", RECALL can sync:

- library-model.json
- quizzes/

into this folder during that browser session.

You can keep this folder beside the app files so the managed quiz library is easy to find.
