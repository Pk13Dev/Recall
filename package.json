{
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node scripts/check-js.js",
    "test": "npm run check && npm run build",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "mobile:preflight": "node scripts/mobile.js preflight",
    "mobile:sync": "node scripts/mobile.js sync",
    "mobile:open": "npx cap open android",
    "mobile:run": "node scripts/mobile.js run",
    "mobile:build": "node scripts/mobile.js build",
    "mobile:doctor": "node scripts/mobile.js doctor",
    "apk": "node scripts/mobile.js apk",
    "apk:debug": "node scripts/mobile.js apk:debug",
    "apk:release": "node scripts/mobile.js apk:release",
    "cert": "node scripts/create-local-cert.js",
    "start:legacy:https": "node scripts/serve-root.js --https",
    "serve:legacy:https": "node scripts/serve-root.js --https --no-open",
    "start:legacy:http": "node scripts/serve-root.js",
    "serve:legacy:http": "node scripts/serve-root.js --no-open"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/cli": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "@tauri-apps/api": "^2.10.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1",
    "vite": "^8.0.10",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
